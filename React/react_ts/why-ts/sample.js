// function sum1(a, b) {
//   return a + b;
// }
//js를 ts처럼 사용하는 법

// @ts-check

/**
 *
 * @param {number} a 첫 번째 숫자
 * @param {number} b 두 번째 숫자
 * @returns
 */
function sum(a, b) {
  return a + b;
}
sum(10, 20);
